{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "python app.py --no-ssl",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
